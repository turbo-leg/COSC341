<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LeaderBoardActivity">

    <ImageButton
        android:id="@+id/backLeaderboard"
        android:layout_width="77dp"
        android:layout_height="64dp"
        android:layout_marginEnd="4dp"
        android:background="@color/white"
        android:onClick="sendBack"
        app:layout_constraintBottom_toBottomOf="@+id/textLeaderboard"
        app:layout_constraintEnd_toStartOf="@+id/textLeaderboard"
        app:layout_constraintTop_toTopOf="@+id/textLeaderboard"
        app:srcCompat="@drawable/outline_arrow_back_24" />

    <TextView
        android:id="@+id/textLeaderboard"
        android:layout_width="243dp"
        android:layout_height="41dp"
        android:layout_marginTop="28dp"
        android:fontFamily="@font/lato"
        android:text="@string/leaderboard"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textColorLink="@color/black"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/line2"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_marginTop="95dp"
        android:background="@android:color/black"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_editor_absoluteX="0dp" />

    <ListView
        android:layout_width="409dp"
        android:layout_height="631dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/line2" />
</androidx.constraintlayout.widget.ConstraintLayout>